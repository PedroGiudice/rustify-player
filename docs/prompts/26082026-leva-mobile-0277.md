# Retomada: validar a 0.2.77 no S24 e fechar a leva mobile

<session_metadata>
branch: main
last_commit: 14f90af (pushado) + docs desta retomada
published: rustify-player_0.2.77.apk + android-latest.json (release dev); rustify-player_0.2.77_amd64.deb (instalado e aberto na cmr-auto)
linear: CMR-210/211/212/213/215/218/220 In Progress (código pronto, validação no aparelho pendente); CMR-221 tech-debt novo
</session_metadata>

## Contexto rápido

Rustify Player (Tauri 2 + SolidJS + Rust; Android em `src/mobile/` + plugin
Kotlin `src-tauri/crates/tauri-plugin-rustify-audio`). Em 26/08 a leva mobile
inteira foi implementada, revisada (4 rodadas adversariais) e publicada como
**0.2.77** (desktop e Android): shuffle de playlist com continuidade off em
todos os caminhos (CMR-211), Queue no tabbar (CMR-213), Recently played
(CMR-215), shuffle do restante da fila (CMR-218), like no mobile com sync e
LWW no desktop (CMR-220), capas por álbum-key (CMR-212). Contexto completo:
`docs/contexto/26082026-leva-mobile-0277.md`.

O que NÃO aconteceu: a validação no S24. O aparelho ficou na tela de bloqueio
(com keyguard o Android corta a rede do app — `blocked=APP_BACKGROUND`) e o
E2E do updater não pôde rodar. Tudo está armado na cmr-auto para rodar
sozinho ao desbloquear.

## Estado do aparelho e da cmr-auto

- S24 na **0.2.76** (sideload via adb), app aberto, `svc power stayon usb`.
  Manifest novo + 565 capas já em `/sdcard/Music/.rustify/` (push feito).
- Tailscale do S24 foi derrubado e religado por adb; o `tun0` estava sem
  passar tráfego pro app antes disso. Se o app não alcançar
  `100.102.249.9:19878`/GitHub com a VPN de pé, reconectar o Tailscale no
  celular (toggle) resolve.
- Journal do S24: 198 eventos represados desde 19/08 (desktop ficou fechado).
- cmr-auto: desktop 0.2.77 rodando (receiver em `100.102.249.9:19878`).
- Vigia `/tmp/wait_unlock_and_e2e.sh` (4 h) roda `/tmp/e2e_updater.py` ao
  detectar `deviceLocked=0`; saída em `/tmp/e2e_updater.out`, estado em
  `/tmp/e2e_updater.state`. Scripts: `/tmp/cdp_eval.py "<js>"`,
  `/tmp/smoke_leva_0277.py` (rodar com `~/.local/bin/uv run --with
  websocket-client`).

## O que fazer

1. **Ler o E2E do updater** (se já rodou): `ssh cmr-auto@100.102.249.9 'cat
   /tmp/e2e_updater.state /tmp/e2e_updater.out'`. Esperado: `check.available`
   true → `install_1: started` → fase `confirm_pending` com o app em background
   → diálogo ao reabrir → cancel → `failed` → retry → tap → `versao_depois
   0.2.77`, `device.json` = `s24`, journal intacto. Se o vigia expirou
   (`timeout`), re-armar: `nohup /tmp/wait_unlock_and_e2e.sh &` e pedir o
   desbloqueio. Se o tap automático não achou o botão, o CEO toca "Atualizar".
2. **Smoke dos 6 itens** (app na 0.2.77, desbloqueado, na Home):
   `ssh cmr-auto@100.102.249.9 'cd /tmp && ~/.local/bin/uv run --with
   websocket-client smoke_leva_0277.py'`. Antes, `lib_rescan` pra ler o
   manifest novo: `cdp_eval.py "window.__TAURI__.core.invoke('lib_rescan')"`.
   Cada `OK`/`FALHOU` tem o dado bruto ao lado; um `FALHOU` = investigar
   (systematic-debugging) antes de qualquer fix.
3. **Sync da semana represada**: com Tailscale de pé e app em primeiro plano
   (ou tocando), em ≤ 60 s o log do desktop (`/tmp/rustify-0277.log` na
   cmr-auto ou o log do app) mostra `sync receiver: lote processado`;
   `curl :16333/collections/track_enrichments/points/<id>` de uma faixa
   curtida no S24 mostra `liked_device: "s24"` e `like_updated_at`.
4. **Promover os scripts** `cdp_eval.py`, `smoke_leva_0277.py`,
   `e2e_updater.py` para `scripts/android/` (ajustando docstrings) e trocar a
   referência ao scratchpad de 13/08 no CLAUDE.md (seção "Smoke test CDP").
5. **Docs e Linear**: preencher a seção "Validação no S24" do contexto;
   fechar CMR-210/211/212/213/215/218/220 (ou registrar o que falhou);
   CMR-221 segue backlog.
6. Depois: CMR-217 (loudness), CMR-214 (favorites reais), CMR-216 (badges),
   CMR-219/220 (ícones — asset do CEO).

## Restrições

- Sem bump/release novo a menos que o smoke ache bug; se achar, acumular e
  soltar 0.2.78 uma vez (desktop antes do APK se tocar no receiver).
- Não mexer em origins/vocabulário do sinal; regras duras do CLAUDE.md
  (async+AppHandle, future de IPC nunca dropado, 503 só em transporte).
- `docs/metrics/regua-*` modificados pelo timer: commitar junto.

## Como verificar

```bash
cd /home/opc/rustify-player
rtk proxy git status --short && rtk proxy git log --oneline -3
rtk proxy npx vitest run 2>&1 | tail -3        # 399
cd src-tauri && rtk proxy cargo test -p tauri-plugin-rustify-audio 2>&1 | grep "^test result" | head -1   # 15
curl -sL https://github.com/PedroGiudice/rustify-player/releases/download/dev/android-latest.json | jq -r .version   # 0.2.77
ssh cmr-auto@100.102.249.9 'adb shell dumpsys package dev.cmr.rustifyplayer | grep versionName; cat /tmp/e2e_updater.state'
```
