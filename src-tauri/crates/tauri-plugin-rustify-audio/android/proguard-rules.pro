# Regras especificas do modulo. O plugin Tauri e instanciado por reflexao
# (PluginManager.load) e o service e resolvido pelo manifest — os `keep` que
# importam vivem em consumer-rules.pro, aplicado tambem no app consumidor.
